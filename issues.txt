UNDESIRED ROUNDED CORNERS
the search bar higlighting border is slightly rounded
the search button is slightly rounded
radius 0 is not working

TEXT WRAPS WHEN WINDOW REDUCED NAV
partly solved using min-width
doesn't work with span (inline)
for it to work with span make span float left and set width. But did not work as expected.

